
```scheme
(define (+ a b)
  (if (= a 0)
	  b
	  (inc (+ (dec a) b))
  ))
  
(+ 4 5)
(inc (+ 3 4))
(inc (inc (+ 2 4)))
(inc (inc (inc (+ 1 5))))
(inc (inc (inc (inc (+ 0 5)))))
(inc (inc (inc (inc 5))))
```

```scheme
(define (+ a b)
	(if (= a 0)
		b
		(+ (dec a) (inc b))
	)
)

(+ 4 5)
(+ (dec 4) (inc 5))
(+ (dec (dec 4)) (inc (inc 5)))
(+ (dec (dec (dec 4))) (inc (inc (inc 5))))
(+ (dec (dec (dec (dec 4)))) (inc (inc (inc (inc 5)))))
(inc (inc (inc (inc 5))))
```