**cond** -- возвращает первое [[Логические константы#^c11ff6|логическое true]]
```scheme
(cond (bool val)
	(else-bool val)
	...
	(else-...-bool val))
```
**if** -- (bool (true-val) (false-val))
```scheme
(if (> 1 2)
	3
	4)
#|4|#
```
