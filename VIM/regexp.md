**ВСЕ КОМАНДЫ ЭКРАНИРУЮТСЯ** `\`
    **Любой** символ `.` не экранируется

### 

### Положение
- По умолчанию всюду
- **<>** — ограничители *слов*. Начало-конец.
- **^$** — ограничители *строки*  Начало-конец

