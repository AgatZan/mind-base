- || (or) - ищет первый true и выводит его
- && (and) - ищет первый false и выводит его
- ! (not) - преобразует в bool и инвертирует результат
- ?? (is null)  - сравнивает с null || undefind если попал, то выводит 2-й операнд иначе себя