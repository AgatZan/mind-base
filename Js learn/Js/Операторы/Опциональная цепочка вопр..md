some?. -если some не определен (null, undefind), то вызовы после ?. не произойдут
?.() -> если вызываемая функция не определена, то  вернется undefind
?.[] -> при обращении к несуществующему элементу массива

